# Nestjs specific
/dist
/node_modules
/build
/tmp

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# dotenv environment variable files
.env
.env.development
.env.test
.env.production

# temp directory
.temp
.tmp

# Monorepo build output (nested, the entries above only match the repo root)
node_modules/
dist/
build/
.next
out
*.tsbuildinfo

# Task + test caches and reports (.auth holds the e2e run's shared session)
.turbo
coverage
playwright-report
test-results
.pnpm-store
.auth

# VS Code: share the settings that make the repo build the same way for
# everyone, keep personal files (launch configs, local tasks) out.
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Local-only overrides and generated TLS material
*.local
certs
.claude/settings.local.json

# OS
.DS_Store
